# MACFE-Firmware-2026

1. After cloning this repository, open the terminal and navigate to the directory containing the source code file: `Hello_World.cpp`

2. Compile the code using the following command to generate an executable named `hello_world`:
   ```bash
   g++ Hello_World.cpp -o hello_world

3. Run the executable:

On Linux/Mac:
   ```bash
   ./hello_world
   ```

On Windows:

``` PowerShell
.\hello_world.exe
